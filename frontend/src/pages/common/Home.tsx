import { Box, Container } from "@mui/material";

const Home = () => {
    return (
        <Box component="section" id="home">
            <Container>Home</Container>
        </Box>
    );
};

export default Home;

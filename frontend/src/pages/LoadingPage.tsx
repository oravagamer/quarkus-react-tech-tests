import {CircularProgress} from "@mui/material";

const LoadingPage = () => {
    return (
        <section id="loading">
            <CircularProgress />
        </section>
    );
}

export default LoadingPage;